return {
  "tris203/precognition.nvim",
  -- event = "VeryLazy", -- Optional: load later if needed
  config = {
    startVisible = true, -- Optional: start with hints visible
    -- showBlankVirtLine = true,
  },
}
